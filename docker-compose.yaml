# Main Docker Compose file - orchestrates all service groups
# Start all services: docker compose --profile all up -d
# Start specific groups: docker compose --profile media --profile networking up -d

include:
  - profiles/networking.yaml
  - profiles/media.yaml
  - profiles/monitoring.yaml
  - profiles/utilities.yaml
  - profiles/home-automation.yaml
  - profiles/immich.yaml
