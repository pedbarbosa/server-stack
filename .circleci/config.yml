version: 2.1

orbs:
  docker: circleci/docker@2.2.0

jobs:
  build:
    docker:
      - image: cimg/base:stable
    steps:
      - docker/install-docker-compose
      - checkout
      - run:
        name: Test
        command: echo "."
