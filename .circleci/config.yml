version: 2.1

orbs:
  docker: circleci/docker@2.2.0

jobs:
  build:
    docker:
        - image: ypereirareis/docker-compose
    steps:
        - checkout
        - setup_remote_docker
        - run:
            name: Test
            command: docker ps -a